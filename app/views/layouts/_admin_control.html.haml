#admin-control
 %span ADMINSTRATOR CONTROL
 %nav#admin-nav
  %li= link_to "Admin Page", rails_admin_path
  %li= link_to "Log Out", rails_admin_path
  - if controller.controller_name == 'projects' && controller.action_name == 'show'
   %li= link_to "Edit this project", edit_project_path(@project)
   %li= link_to "Delete project", {  :action => 'destroy', :id => @project.id , :controller => 'projects'}  ,:id => "delete",:method => :delete, :confirm => "Are you sure you want to delete #{@project.project_name}?"
