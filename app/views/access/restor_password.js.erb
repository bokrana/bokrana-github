var $modal = $('#modal');
  $modal.html("<%= escape_javascript(render 'access/restor_password') %>").promise().done(function(){
  $modal.modal({ "width" : "575px" , "focusOn" : "#restore-email" });
  $("#restore-email").focus();
 });


$modal.on('click', '#new-account-link-login-form', function(){
  $modal.modal('loading');
});

$modal.on('click', '#have-login', function(){
  $modal.modal('loading');
});



$.validate({
   form : '#restore-form'
  });

