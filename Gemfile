source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.0'

#Look up street addresses, IP addresses,and geographic coordinates
gem 'geocoder'

#rails web server
gem 'thin'

#Pg is the Ruby interface to the {PostgreSQL RDBMS
gem 'mysql2', '~> 0.3.18'

#alternative to HTML
gem 'haml-rails' 

# Use SCSS for stylesheets
gem 'sass-rails'

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier'

#RailsAdmin is a Rails engine that provides an easy-to-use interface for managing your data.
gem 'rails_admin'

#Track changes to your models, for auditing or versioning
gem 'paper_trail', '~> 4.0.0'

#Easy file attachment management for ActiveRecord ( upload files )
gem "paperclip", :git => "git://github.com/thoughtbot/paperclip.git"

#Remotipart is a Ruby on Rails gem enabling AJAX file uploads with jQuery
gem 'remotipart', '~> 1.2'

#Add a comment summarizing the current schema to the top of each ActiveRecord model source file : to use it :# bundle exec annotate --position before
gem 'annotate'

#Tagging system i installed
gem 'acts-as-taggable-on' 

#create URI-friendly -- alternative of friendly_id gem
gem "stringex"

# Kaminari is a Scope & Engine based, clean, powerful, agnostic, customizable and sophisticated paginator for Rails 
gem "kaminari"

#Dump (parts) of your database to db/seeds.rb to get a headstart creating a meaningful seeds.rb file ---rake db:seed:dump
gem "seed_dump"

#easily generate fake data: names, addresses, phone numbers, etc
gem "faker"

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder'

#Forms made easy!
#gem 'simple_form'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
gem 'sprockets-rails', '2.3.3'
